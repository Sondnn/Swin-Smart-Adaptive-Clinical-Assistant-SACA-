<Window x:Class="SACA.WindowsApp.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="SACA - Clinical Assistant"
        Height="720"
        Width="1000"
        WindowStartupLocation="CenterScreen"
        Background="{StaticResource AppBackgroundBrush}">

    <Grid>
        <ContentControl x:Name="MainContent"/>

        <StackPanel x:Name="LanguageSwitcher"
                    Orientation="Horizontal"
                    HorizontalAlignment="Right"
                    VerticalAlignment="Top"
                    Margin="0,16,16,0"
                    Panel.ZIndex="10">
            <Button x:Name="EnglishButton"
                    Content="English"
                    Width="108"
                    Height="38"
                    FontSize="14"
                    Padding="10,6"
                    Margin="0,0,8,0"
                    Click="EnglishLanguage_Click"/>
            <Button x:Name="WalmajarriButton"
                    Content="Walmajarri"
                    Width="118"
                    Height="38"
                    FontSize="14"
                    Padding="10,6"
                    Click="WalmajarriLanguage_Click"/>
        </StackPanel>
    </Grid>
</Window>
